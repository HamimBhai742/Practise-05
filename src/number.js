const add = (num1, num2) => {
  return num1 + num2;
};
export default add;

export const subtract = (num1, num2) => {
  return num1 - num2;
};

export function multiply(num1, num2) {
  const result = num1 * num2;
  console.log(result);
}
